// 'use strict';

// const convertBuffer = require('../lib/files.js');
// console.log(typeof convertBuffer);

// const events = require('../lib/events.js');

// const emit = jest.spyOn(events, 'emit');

// describe('`convertBuffer` function', () => {
//   it('should transform its argument text to uppercase', () => {
//     const text = 'test';
//     const result = 'TEST';
//     expect(convertBuffer(text)).toBe(result);
//   });
// });